<div class="well">
  <%= link_to "#{content_tag(:i, '', :class => "icon-arrow-left")} #{t('admin.misc.back')}".html_safe, admin_company_types_path, :class => "btn pull-right" %>
</div>

<%= simple_form_for([:admin, @company_type], :html => { :class => 'form-horizontal' })  do |f| %>
	<%= f.error_notification %>
	
	<fieldset>
    <%= f.input :name %>
		<%= f.input :sequence %>

		<div class="form-actions">
			<%= f.button :submit, :class=> "btn btn-primary" %>
		</div>
	</fieldset>
<% end %>