<%= simple_form_for([:admin,@procedure_roleship], :html => {:class =>"form-horizontal"}) do |f| %>
  <% if @procedure_roleship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@procedure_roleship.errors.count, "error") %> prohibited this procedure_roleship from being saved:</h2>

      <ul>
      <% @procedure_roleship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :role_id, @role_id %>
  <div class="control-group">
    <label id="trk-admin-group-setting-name" class="control-label alert alert-info" for="group_id[]">显示当前组名</label>
    <div class="controls">
      <%=raw procedure_role(@procedure_roleships) %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit '保存', :class => "btn btn-primary" %>
  </div>
<% end %>
