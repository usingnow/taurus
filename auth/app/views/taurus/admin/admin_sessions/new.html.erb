<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">采恪企业管理</a>
      <a class="brand" href="#">Tracker boosts your e-Business</a>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="span9">
      <div class="hero-unit">
        <h2>The latest updates are always ready for you!</h2>
        <h2>赶快尝试新版本！</h2>

        <p>
          New features for the release of 1.3.4
          新版本带来新的体验！
        </p>
        <p>
          <a class="btn btn-primary btn-larger">了解更多
            <i class="icon-play"></i>
          </a>
          <a class="btn btn-primary btn-larger">立即下载
            <i class="icon-download-alt"></i>
          </a>
        </p>
      </div>
    </div>
    <div class="span3">
      <%= form_for(resource, :as => resource_name, :url => administrator_session_path, 
                   :html => {:class => "well"}) do |f| %>
          <%= f.label :email %>
          <%= f.text_field :email, :class => "span2" %>

          <%= f.label :password %>
          <%= f.password_field :password, :class => "span2" %>

          <% if devise_mapping.rememberable? -%>
            <label for="administrator_remember_me" class="checkbox">
              <%= f.check_box :remember_me %>记住我
            </label>
          <% end -%>

          <%= f.submit "登录", :class => "btn btn-primary" %>
      <% end %>
      
      <% unless alert.nil? %>
          <div class="alert alert-block alert-error fade in"><%= alert %>
            <a class="close" data-dismiss="alert" href="#">&times;</a></div>
      <% end %>
    </div>
  </div>
  <hr />
  <p>Powered by &copy;Tracker</p>
</div>


