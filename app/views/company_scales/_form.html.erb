<%= simple_form_for(@company_scale) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :sequence %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
