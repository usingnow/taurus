<%= form_for([:admin,@sku_image],:html => {:multipart=>true}) do |f| %>
  <% if @sku_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sku_image.errors.count, "error") %> prohibited this sku_image from being saved:</h2>

      <ul>
      <% @sku_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= file_field_tag :image_url %>
  </div>
  <div class="field">
    <%= f.label :image_description %><br />
    <%= f.text_area :image_description %>
  </div>
  <div class="field">
    <%= f.label :sort %><br />
    <%= f.text_field :sort %>
  </div>
  <div class="field">
    <%= f.label :is_main %><br />
    <%= f.check_box :is_main %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
