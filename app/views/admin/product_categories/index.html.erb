<h1>素材商品分类列表</h1>

<table>
  <tr>
    <th><%= t('attributes.product_category.number') %></th>
    <th><%= t('attributes.product_category.name') %></th>
    <th><%= t('attributes.product_category.description') %></th>
    <th><%= t('attributes.product_category.updated_at') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_categories.each do |product_category| %>
  <tr>
    <td><%= product_category.number %></td>
    <td><%= product_category.name %></td>
    <td><%= product_category.description %></td>
    <td><%= product_category.updated_at %></td>
    <td><%= link_to 'Show', admin_product_category_path(product_category) %></td>
    <td><%= link_to 'Edit', edit_admin_product_category_path(product_category) %></td>
    <td><%= link_to 'Destroy', admin_product_category_path(product_category), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新建素材商品分类', new_admin_product_category_path %>
